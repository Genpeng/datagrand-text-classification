
# 1. Train word embeddings
python train_word_embeddings.py

# 2. Re-save the file of word embeddings to `npy` format
python resave_word_embeddings.py

# 3. Transform the words (or characters) of all the samples to its ids
python transform_data_text.py